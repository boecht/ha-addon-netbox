name: "NetBox"
version: "0.1.0"
slug: "netbox"
description: >-
  NetBox bundled with PostgreSQL and Redis for Home Assistant power users.
arch:
  - amd64
  - aarch64
url: "https://github.com/boecht/ha-addon-netbox"
startup: services
boot: auto
init: false
panel_icon: mdi:server-network
image: "ghcr.io/boecht/ha-addon-netbox/netbox-{arch}"
ports:
  8000/tcp: 8000
ports_description:
  8000/tcp: NetBox web UI
watchdog: "http://[HOST]:[PORT:8000]/health/"
ingress: false
host_network: false
apparmor: false
map:
  - type: share
    read_only: false
  - type: config
    read_only: false
options:
  superuser_username: admin
  superuser_email: admin@example.com
  superuser_password: admin
  allowed_hosts:
    - "*"
  housekeeping_interval: 3600
  enable_prometheus: false
  plugins: []
schema:
  superuser_username: str
  superuser_email: email
  superuser_password: password
  allowed_hosts:
    type: list
    items:
      - type: str
  housekeeping_interval: int(300,86400)
  enable_prometheus: bool
  plugins:
    type: list
    items:
      - type: str
